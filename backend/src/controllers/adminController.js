import User from '../models/User.js'; import Company from '../models/Company.js'; import Job from '../models/Job.js'; import Application from '../models/Application.js'; import { asyncHandler } from '../utils/asyncHandler.js';
export const dashboard = asyncHandler(async (_req, res) => { const [users, students, recruiters, companies, jobs, applications, recentApplications] = await Promise.all([User.countDocuments(), User.countDocuments({ role: 'student' }), User.countDocuments({ role: 'recruiter' }), Company.countDocuments(), Job.countDocuments(), Application.countDocuments(), Application.find().sort('-createdAt').limit(10).populate('student', 'name email').populate('job', 'title').lean()]); res.json({ success: true, statistics: { users, students, recruiters, companies, jobs, applications }, recentApplications }); });
